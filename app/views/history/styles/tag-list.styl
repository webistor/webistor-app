@import 'nib'

.tag-list
  display:block

  .tag-row
    clearfix()

    .tag
      display:block
      float:left

    .num
      display: block
      float: right
      line-height: 30px
      color: #999
      font-weight: 500
      font-family: 'Lato', sans-serif
      cursor: pointer
