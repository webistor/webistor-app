Collection = require 'models/base/collection'
{{#camelize}}{{name}}{{/camelize}} = require './{{name}}'

module.exports = class {{#camelize}}{{pluralName}}{{/camelize}} extends Collection
  path: '{{pluralName}}'
  model: {{#camelize}}{{name}}{{/camelize}}
